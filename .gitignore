node_modules/
screenshots/
scripts/current.pine
temp_*
*.log
discovery-log.json

# Dual Trend Desk — licensed third-party research and everything derived from it.
# Only the pipeline code is tracked; sources, extracted text, curated data and the
# built dashboard stay local.
dt-research-desk/index.html
dt-research-desk/ingest/corpus/
dt-research-desk/ingest/txt/
dt-research-desk/ingest/data/*.json
dt-research-desk/ingest/state.json
dt-research-desk/ingest/build-manifest.json
dt-research-desk/ingest/.pipeline.lock
__pycache__/
*.pyc

# APEX terminal — generated output and the HTTP cache.
# vendor-dist/ (the Perplexity build) and lib/ are the tracked inputs.
# state.json IS tracked — it carries the Artifact URL each bake republishes to.
apex-terminal/cache/
apex-terminal/public/
apex-terminal/report/
apex-terminal/build-manifest.json
